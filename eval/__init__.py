"""Panella eval bundle — NOT part of the shipped package (see pyproject.toml's explicit
allowlists; this directory ships in neither the wheel nor the sdist, and .dockerignore keeps it
out of built images). Reproducible research harness only: `make eval-*`."""
