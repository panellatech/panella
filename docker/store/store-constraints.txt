# store-constraints.txt — C0-3b image slimming: lock the embedding / ML stack to CPU-only,
# reproducible versions. Primary job: pin torch to the +cpu build so the mcp-memory-service
# install never resolves the CUDA torch wheel and its ~3.5GB nvidia/triton dependency closure.
# The remaining pins lock the embedding-determining stack to fixed versions (not hashes, and not the
# full transitive closure — torch's own infra deps stay at whatever the indexes serve). Resolved
# 2026-07-16 against mcp-memory-service==10.67.1 on python:3.12-slim (pip check clean). These exact
# pins are coupled to that version: re-resolve whenever the mcp-memory-service pin is bumped (a stale
# pin here would surface as a hard resolver conflict at build time, never a silent drift).
torch==2.13.0+cpu
sentence-transformers==5.6.0
transformers==4.57.6
tokenizers==0.22.2
huggingface-hub==0.36.2
hf-xet==1.5.1
safetensors==0.8.0
numpy==2.5.1
scipy==1.18.0
scikit-learn==1.9.0
sympy==1.14.0
networkx==3.6.1
mpmath==1.3.0
onnxruntime==1.27.0
regex==2026.7.10
tqdm==4.68.4
filelock==3.29.0
fsspec==2026.4.0
joblib==1.5.3
threadpoolctl==3.6.0
